import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.registrocredito.web',
  appName: 'RegistroCredito.Web',
  webDir: 'dist/registro-credito.web/browser'
};

export default config;
